// automatically injected code from Vite - import of react + App
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import App from './App.jsx'


// automatically injected code from Vite - root to send components to index.html
createRoot(document.getElementById('root')).render(
    <StrictMode>
        <App />
    </StrictMode>,
);